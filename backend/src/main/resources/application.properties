# Server Configuration
server.port=8081

# TODO: Configure the issuer URI where the Access Tokens are issued from
spring.security.oauth2.resourceserver.jwt.issuer-uri="issuer-uri"
# TODO: Configure the jwk-set-uri to an ACCESSIBLE uri for fetching the JSON Key Set Keycloak uses for signing Tokens
spring.security.oauth2.resourceserver.jwt.jwk-set-uri="jwt-set-uri"